// a list of images and thier respective IDs
// the IDs can be anything, as long as they're distinct.
// this array of images will be "registered" with the javascript code in bindings.rs
pub const IMAGES: [(&str, usize); 1] = [("icons8-cheese-64.png", 12345)];
