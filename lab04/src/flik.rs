#[allow(unused_variables)]
#[allow(unreachable_patterns)]

/// An Integer tester created by Flik Enterprises.
/// Uses cutting-edge match technology.
pub fn is_same_number(a: u64, b: u64) -> bool {
    match b {
        a => true,
        _ => false,
    }
}
